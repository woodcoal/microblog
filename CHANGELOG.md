# 更新日志

## [未发布]

### Changed

- 论坛首页左侧“首页”入口改为与板块菜单一致的分组样式；主内容在讨论列表前增加分级板块导览，按一级分组展示二级板块、介绍与真实帖子数量，空分组和空板块均有明确状态。

- 账户双栏壳扩展到与站点头部相同的 1320px 断点和水平内边距；“个人／设置”菜单补充分组卡、图标、悬停与当前态，窄屏仍保持无圆角的横向导航。

- 修正首页“内容推荐”异步节点未携带 Astro 样式作用域而导致的错乱：推荐内容现在按微博动态、论坛三段式主题和博客文章行分别展示；Hero“正在被讨论”改为桌面双列紧凑卡片、移动端单列，避免过宽的横向列表。

- 设置、收藏、关注与粉丝页现在复用同一账户双栏壳：左侧固定为资料摘要、个人入口与设置入口两组菜单，右侧保持统一内容工作区；个人主页资料头恢复头像跨越封面与资料区、资料信息从头像下方单独起行的模板结构。

- 微博与博客频道左栏现仅保留推荐、热门、最新三项内容入口；个人主页为本人提供“收藏／关注／粉丝”标签，分别查询真实的收藏微博与关系数据；全站内容选项卡统一为普通下划线当前态，取消胶囊式圆角选中效果。

- 首页“为你准备”推荐区严格回归 `home-recommendations.html` 原稿的 Hero 实时讨论、紧随 Hero 的推荐区、兴趣面板和内容/创作者双栏节奏；同时接入冷启动兴趣选择、推荐理由、独立骨架/空态/错误重试与可恢复关注反馈。

- 优化搜索交互与结果展示：顶部全局搜索框补充 `🔍` 图标；自动完成下拉限制高度并对过长文本省略；搜索页移除左右侧栏，改为居中的单列结果区。综合结果按创建时间合并排序并采用类型、内容、作者、时间四列表格，内容优先标题、无标题时显示正文，最长 100 字且按宽度省略；微博、论坛和博客搜索分别复用对应频道的列表展示。

- 账户、关系与通知页继续对齐 A 型 UX 基线：认证页与头部共用 1320px 内容断点，桌面导语与表单贴两端分列、移动端收为单列，并采用轻量表单阴影；个人主页按模板改为资料与帖子主栏、个人菜单／最近关注／推荐话题右栏，头像跨越封面与白色资料区、资料信息纵向排在头像下方，内容流只按动态／文章／讨论模块切换而不再提供综合结果；微博个人菜单的收藏继续指向仅展示已收藏微博的真实列表；设置页改为用户中心左侧菜单与右侧编辑区，个人入口复用微博的收藏／关注／粉丝／我的主页分组、顺序与图标，移动端可横向滚动；认证与设置反馈补齐读屏语义，关注/粉丝改为带真实关注操作的资料行与切换导航，通知筛选明确为导航区域并避免未读状态重复提交。账户、关系和通知页面不再复用微博频道侧栏。

- 未配置 `SITE_MODES` 时默认同时启用微博、论坛与博客，与示例配置和公开文档一致；未登录访问 `/blog/write` 会携带安全的站内返回地址跳转登录，登录成功后回到创作页。

- 修复 MT-86 旧版热门配置迁移：准确识别仅含点赞、评论与衰减小时的三字段配置，以等价的 v2 半衰期迁移并使用默认收藏权重；全零权重整体回退，半衰期支持 1 至 720 小时。

- 统一热门推荐为 MT-86 v2 公式（对互动取对数、权重 1/2/3、48 小时半衰期），全局置顶移出 200 条候选窗口；冷启动与混合推荐均使用显式探索配额。

- 微博列表与详情页现在复用统一的作者信息与互动文字组件：发布时间紧随 `@用户名` 显示，评论、喜欢和收藏均以文字入口呈现，同时保留原有点赞、收藏及评论跳转行为。

- 论坛列表、版块页与帖子详情的移动导航移除独立“首页”入口，仅保留按一级分类组织的板块导航；桌面与折叠侧栏入口不受影响。

- 论坛移动导航仅显示实际配置的板块图标，不再以名称首字占位；一级分类及其板块入口改为不可拆分的横向分组，只有整个分类组无法容纳时才换行。

- 论坛普通侧栏恢复纯文字板块入口，图标／首字仅在折叠侧栏或移动端显示；列表页在手机端将论坛导航移至内容上方，论坛列表与详情页均按一级分组换行展示，一级名称字号略高于二级入口。

- 修复桌面端用户下拉菜单定位：弹层继续贴齐用户入口右侧，菜单项内容明确左对齐，避免被通用导航样式居中覆盖。

- 论坛列表、版块页及详情页的左侧菜单移除“浏览论坛／论坛板块”说明文案；中屏折叠菜单为未设图标的版块使用名称首字，并为首页和全部版块入口补充完整名称提示与可读名称。

- 论坛详情的作者信息行与标题拉开轻量间距，并以低饱和主题色背景和边框区分；390px 以下保留论坛首页及板块入口为可横向滚动的文字导航，不再直接隐藏左侧菜单。

- 论坛详情改为复用频道版块菜单的两列布局，移除右侧辅助栏；主题卡内按“版块 · 讨论主题、标题、作者与关注/收藏操作”重排，正文互动与楼层讨论保持可用。

- 全局导航移除与用户菜单重复的桌面通知入口；移动端汉堡菜单不再显示主题设置；用户下拉菜单改为左对齐并缩小至 168px。

- 补充博客热门流自定义分类透传回归测试，保护统一热门 DTO 与文章列表的 `customCategory` 契约。

- 博客详情的分类标识改为优先显示作者自定义分类且仅在标题上方展示一次；作者信息行补齐喜欢、收藏与讨论计数，并移除正文底部重复的博客互动统计；右栏在有章节时先显示目录，文章底部新增同作者公开博客的上一篇／下一篇链接。

- 开发、构建及生产启动流程现在会先生成 Prisma Client 并应用已提交迁移，避免新字段已进入应用代码但运行时 Client 仍为旧版本而导致发布 500；新增可单独执行的 `npm run db:prepare`。

- 博客编辑器恢复“自定义分类”输入框：作者可在新建和编辑文章时填写最多 50 个字符的个人分类；它与管理员维护的系统分类互斥、随草稿保存，并在文章列表、详情页和 v1 API 中显示。SQLite/MySQL 均新增对应迁移，服务端拒绝非博客或同时提交两类分类的请求。

- 博客阅读列表的文章行严格按四层信息排列：首行仅显示分类，随后依次为标题与摘要，末行集中展示作者、发布日期、喜欢数和评论数。

- 新增首页登录态推荐体验规格与可交互原型：明确“全站动态优先、为你准备次之”的内容层级，覆盖可跳过兴趣选择、内容/创作者推荐理由、独立加载/空态/失败重试、关注反馈、桌面与移动端以及可访问性验收；仅定义后续实现边界，不改变既有推荐算法或用户关系。

- 统一微博、论坛和博客的列表页标题区：登录态发布按钮固定贴齐右上角，标题导语保持左侧纵向阅读节奏；论坛的版块列表同步使用该对齐轴，微博“最新”列表复用频道导语与发布入口；移动端仍按原有顺序纵向堆叠。

- 微博左侧导航恢复“推荐／热门”入口；热门流沿用既有互动计数排序。收藏、关注、粉丝列表在宽屏保留三列壳，右栏展示热门话题与对应的真实作者或关系条目。

- 微博列表侧栏按“动态／个人（或账户）”分组，并补充登录用户的个人主页入口；768–1199px 收起发现栏并将左栏折叠为图标导航，确保主信息流不被三栏挤压。

- 论坛编辑器的版块选择改为按一级分组展示；只有没有子分类的末级版块可选。版块详情页仅在访问末级版块时预设分类，服务端同步拒绝向非末级版块发布或改帖。

- 登录与注册页的认证容器扩展至双栏设计宽度，桌面端左侧站点文案不再因 760px 内容上限而过早换行；表单列与移动端布局保持不变。

- 博客新建与编辑页现在使用相同的宽工作区；编辑态不再受窄用户布局挤压。Markdown 编辑器工具栏改为统一的紧凑图标分组，补充撤销/重做、链接面板与完整键盘/读屏提示，图片上传、粘贴、草稿及 Markdown 存储契约保持不变。

- 全局操作提示改用原生顶层对话框，分类、用户、帖子等管理编辑器提交失败时不再被当前编辑弹窗遮挡；结构化错误响应统一提取为可读提示，避免直接显示 JSON 内容。

- 内容浏览列表统一复用三栏内容壳：补齐最新、标签与搜索页的发现右栏；微博、论坛和博客改为可组合的真实数据发现区块，热门内容不再与中心列表重复，缺少发现数据时自动收起右栏。

- 微博、论坛和博客详情页统一补齐顶部导航面包条，作者操作区与其同行并贴右显示，避免影响卡片内作者/时间对齐；置顶／取消置顶、编辑、删除和版本历史继续遵循各频道原有显示条件，并复用既有确认与服务端校验。

- 详情页评论默认按最新优先的倒序展示；正序/倒序切换仅向登录用户显示，保存成功后按其偏好刷新。

- 修复微博编辑器点击“附件”时事件冒泡额外触发图片文件选择器的问题；附件选择现在仅打开一次对应的原生选择器。

- 新增 `docs/api/agent.md`：以独立 SQLite 数据库实际调用全部 15 个 `/api/agent` 方法级端点，记录前置条件、请求示例、完整纯文本响应、认证/校验分支与测试数据清理结论。

- 修复缺失的 `.sr-only` 无障碍工具类，顶部搜索标签不再与 placeholder 重复可见；博客首页和分类页左侧导航移除与首页重复的“最新”入口。

- 根据详情复验修正博客与论坛的内容轴：主列扩展至与头部相同的宽度断点；博客右栏增加连续竖线分隔，作者操作移入右栏；论坛的版块导航移至圆角主题卡外、卡内标题上方显示版块名称。详情及评论互动在博客/论坛中改用“喜欢／收藏／讨论（回复）”中文文案。

- 恢复全局导航与搜索页输入框的防抖实时建议；建议按绑定输入框独立处理并防止过期响应覆盖，论坛、博客、微博内的顶部搜索会预设对应频道筛选并优先展示同频道建议。搜索页“综合”现在同时展示内容和用户结果，Tab 改为普通下划线当前态，顶部输入仅保留 placeholder 文案。

- 论坛列表改为 `forum-a.html` 的头像、讨论信息与回复数三段式行；版块页的面包屑、名称、介绍和发帖操作改为与论坛首页一致的纵向文案区。博客首页及分类列表改为 `blog-a.html` 的分类日期、标题、摘要、作者统计分隔文章行，并补齐分类页的三列导航壳。

- 搜索页切换为 `search-a.html` 对应的简洁单列搜索壳：保留真实搜索、频道/用户筛选和游标分页，展示为综合、微博、论坛、博客、用户主标签与分隔结果行。除搜索页外，全站导航右侧统一为可直接提交的搜索输入框。

- 微博列表严格限定为微博内容：最新、标签、收藏及关系页动态不再混入论坛或博客；收藏与关注菜单顺序对换，微博列表滚动到底自动追加下一页并保留无脚本分页降级。帖子互动统一为左侧点赞、收藏、评论三个同型入口。

- 论坛首页与版块页统一为三列内容壳：左侧增加频道首页和板块导航，版块页显示面包屑、名称/介绍和最新/热门/精华排序；论坛列表补齐分页。博客导航增加首页与博客收藏筛选，文章阅读列表继续严格限定博客内容。
- 内容详情按各自 A 模板重构：博客使用 720px 阅读列与“相关博客／目录”右栏，论坛使用“相关版块／作者信息”右栏及楼层讨论流，微博恢复频道相同的左侧菜单、动态卡片主列和“作者／其他微博”右栏；三种布局在 1024px 以下收紧、767px 以下按信息顺序下移。
- 博客新建与编辑统一复用 `blog-editor-a` 的 760px 文稿列、230px 发布设置列、无卡片衬线正文和边框格式工具带，保留原有分类、可见度、草稿、上传与保存契约。

- 根据复验反馈统一三频道为左侧导航、中间列表、右侧真实推荐的桌面三列壳；论坛保留版块导航，博客提供最新/推荐/热门入口。帖子底部将互动按钮归入左侧组，可见性（含加密帖子）说明固定至右侧。

- 微博图片区域支持 `WEIBO_MEDIA_MAX_WIDTH_PX` 环境变量（默认 640px），列表与详情统一为最多 3×3 的响应式画廊，单图保持原比例且最大为配置宽度的三分之一；微博、论坛、博客首页顶部文案纳入既有站点文案后台管理。

- 修正频道列表验收问题：微博菜单首页固定指向 `/weibo`，无发现侧栏的时间线扩展为可用内容宽度；帖子媒体统一限制为 3×3 网格、单图保留原始比例并通过 `--post-media-max-width` 预留配置入口，点赞/评论/收藏操作保持单行可横向滚动。论坛首页改为版块导航与可排序帖子流，博客列表取消左侧空列并按阅读列表对齐；关系页移除装饰性边框。

- 列表页补齐可选发现侧栏：微博展示真实热门标签，论坛展示真实版块入口，博客展示真实分类入口；宽屏遵循 A 型内容壳，1024px 以下自动收起，主内容与移动端导航保持可用。搜索筛选的主色表面改用前景色令牌，用户简介高亮继续以转义后的文本渲染。

- 为主色背景新增独立的默认与悬停前景色令牌；首页 CTA、特色频道卡、移动端注册入口及其他主色表面均改用该令牌，并以 4 种主题 × 5 种强调色的 WCAG AA 对比度回归测试约束组合。

- 新增后台“站点文案”入口，管理员可编辑、预览并保存首页 Hero、登录页和注册页的固定 Markdown 文案；三个前台展示位统一使用服务端受限渲染 HTML，并在读取异常或未初始化时保留代码内默认文案。

- 首页登录后的“为你推荐”改为帖子与用户双栏：两路数据独立处理加载、空态和失败重试；关注推荐用户成功后会从候选列表移除，并在窄屏按帖子、用户顺序堆叠。

- 新增固定站点文案服务：仅支持 `home.hero`、`auth.login.intro`、`auth.register.intro`，管理员更新以事务同步当前值与不可变审计版本；公共读取提供安全渲染 HTML 和代码内默认文案降级。

- 首页导航在首页壳内采用 `--layout-home` 宽度，与首页内容保持同一水平轴；其他页面仍沿用既有导航宽度。

- 新增鉴权 Action `getRecommendUsers`：为首页右栏返回最近 90 天有公开帖的未关注用户，并按共同关注、公开发帖数、粉丝数、最新公开帖和用户名稳定排序；同步增加 SQLite/MySQL 候选查询索引。
- 修复推荐候选上限在完整业务排序后截断，并增加高分尾部候选与未登录 Action 回归测试；Post 索引改为以 `userId` 开头，并补齐 Follow 反向关注索引。
- 将推荐候选的帖子、粉丝与共同关注统计改为独立相关子查询，避免多对多明细并联造成行数乘法；新增 100×100×100 规模的查询计划与性能回归。
- 对齐 C 型发现首页的频道卡标题层级、桌面搜索入口与移动端 CTA；全局导航、主题切换和菜单控件统一采用 44px 最小触控目标及主题化焦点环。
- 直接改造登录、注册、搜索、通知、三频道列表与详情、关系、写作、设置及全部后台路由的页面语义结构；各页现在具备对应模板的导语、标题锚点和独立工作区，而不再仅依赖共享样式覆盖。
- 根据 `docs/ux` 的页面样本细化首页、三频道、搜索、通知、个人、账户、关系、编辑与后台页面的视觉层级和响应式布局。
- 根据 UX 基线更新全局页面壳、导航、内容列与控件尺寸，并保留所有既有主题和强调色。
- 首页改为 C 型发现页，增加引导区、频道入口和受控宽度的热门内容流。
- 管理后台调整为浅色左轨、白色数据工作区，并限制宽表仅在自身容器内横向滚动。

### 2026-08-05

**变更类型**: 数据库迁移 / 优化

- 保留 SQLite/libSQL 与 MySQL/MariaDB 两种 Prisma driver adapter，通过 `DATABASE_PROVIDER` 选择 schema、迁移和运行时连接；切换前必须备份并自行迁移数据。
- 将 SQLite 的用户备注、分类/收藏/帖子模式和阅读记录增量迁移合并为独立 `0_init` 基线；MySQL 保留对应的独立初始基线，消除两种 provider 的迁移锁冲突。

### v1.26.805 — 2026-08-05

**变更类型**: 版本发布

- 升级 Astro 至 7，并保留 `/api/agent` 自动化接口；同步修复其认证、兼容字段、错误状态和通知分页等契约缺陷。
- 修复 API 文档页使用 CDN latest 导致 Scalar 无法初始化的问题，改为打包项目内的 Scalar 1.x 依赖并保留 v1/Agent 文档切换。

### 2026-08-05

**变更类型**: 重构 / 优化 / 安全加固

- 将 `/api/agent` 明确为长期保留的自动化 Agent 纯文本接口：统一 Bearer-only 认证，兼容 `imageUrls`/`images` 与 `mutual` 旧别名，修复发帖错误状态映射和通知页码失效，并新增真实 HTTP 验收测试及双 API 定位文档。

- 升级 Astro 至 7.1.6、`@astrojs/node` 至 11.0.3、`@astrojs/react` 至 6.0.2（两集成版本遵循 Astro 自身的独立版本号），并显式设置 `compressHTML: true` 保持既有 HTML 压缩行为。
- 新增 `/api/v1` 版本化 JSON REST API，覆盖认证、帖子、评论、点赞、关注、时间线、搜索和标签读取；复用既有认证、Service 与全局 API 安全中间件。

- 新增 `API_V1_ENABLED` 与 `API_AGENT_ENABLED` 配置开关，默认启用；禁用时由全局中间件在 CORS、限流和路由前分别返回 v1 JSON 或 Agent 纯文本 404 响应。

- 修复 `/api/v1` 可见性读取、重复删除 404 语义与 Bearer-only 鉴权；password 列表正文脱敏、详情支持 `password` 查询参数，并补齐迁移说明与 HTTP 验收矩阵。

- 将外部 OpenAPI 契约收敛到版本化 `/api/v1` MVP，明确公开读与 Bearer 写操作边界、统一错误响应、DTO 适配规则及切换操作的重试风险；管理后台继续仅使用 Astro Actions。
- 管理后台用户管理新增「添加用户」对话框与 `createUser` Action；管理员可绕过前台注册开关创建 user 或 admin，且沿用注册的用户名、邮箱、密码与唯一性校验。
- 移除 DaLi.Lens 外部推荐依赖，改用本地热门排序、标签/分类匹配及本地阅读历史。
- 修复本地推荐的分层架构、候选查询数据放大、永久排除已读和相似源帖权限校验问题。
- 修复 Markdown URL、搜索建议和通知动态内容的持久型 XSS 风险。
- 新增 `lint` 与 `typecheck` 工程脚本，统一格式和 Astro 类型检查入口。
- 将 `content.service.ts` 与 `user.service.ts` 的 Prisma 查询下沉至 `lib/post.ts`、`lib/comment.ts` 与 `lib/user.ts`，保持既有 Service 接口不变。
- 新增公共 `escapeHtml` 工具，并用于 Token、Webhook 及编辑器上传文件预览中的动态 HTML 值。
- 新增 `HOT_SORT_CANDIDATE_WINDOW` 配置，默认保留 200 条热门排序候选（必须为正数，无效值回退默认值 200）。
- 新增 ESLint flat config；Actions 统一从 `astro:schema` 迁移到 `astro/zod`。
- 补齐认证 helper、浏览器全局、FormData、可空 DOM 节点和帖子卡片的类型契约。
- 修复管理端单条用户、帖子、评论操作仍调用已移除 Action 的功能错误，统一复用管理员批量 Action 的单项调用。
- 修复分类父级更新契约、帖子分类空值收窄、DOM 查询类型及 Markdown renderer 的新版类型兼容。
- 修复微博、论坛与博客编辑器回显/草稿内容的前导空白；补齐博客编辑器直接使用的 `@tiptap/core` 依赖声明。
- 修复设置页多行文本域的模板缩进写回，以及微博、论坛弹窗历史草稿的前导空白恢复。
- 统一使用 Astro `set:text` 填充动态文本域，从机制上避免模板换行和缩进再次注入前导空白。
- 统一微博创建与编辑为 `PostEditor.astro`，支持编辑态内容、可见度及既有图片/附件回显、移除和追加上传，并保留 emoji、草稿、拖拽和粘贴图片上传。
- 修复 emoji 选择器定位：按触发按钮坐标显示，并支持点击遮罩或选择表情关闭。
- 三类编辑器统一通过 `actions.uploadMedia` 上传图片；论坛和博客在上传失败时显示可见错误提示。
- 统一博客新建和编辑页面的文档工作区：两者均使用顶部轻工具栏、居中文档页和无边框富文本编辑器，并复用保存事件、分类/可见度控件和独立草稿键。
- 修复三类编辑器提交成功后的草稿生命周期：取消待执行的防抖保存、清除对应 localStorage，并将编辑器恢复为空状态；提交失败时保留草稿。博客仅在创建/更新成功后触发草稿清除，创建态与编辑态草稿键继续隔离。

**残余事项**：其余为未使用声明、弃用 API 和 ESLint 存量规则治理项。

### 2026-06-21 19:30

**提交人**: AI
**提交哈希**: ab499e9
**影响范围**: actions 层（3 文件）、api 层（10 文件）、services 层（5 文件新增/扩展）、lib 层（3 文件扩展）
**变更类型**: 重构

**详细描述**:

- 重构 actions/content.ts：createPost/updatePost/deletePost 业务逻辑下沉到 content.service.ts
- 重构 actions/settings.ts：changePassword/uploadAvatar/updateCommentSort 下沉到 settings.service.ts
- 重构 actions/misc.ts：renderMarkdown 下沉到 misc.service.ts
- 重构 api/agent/posts/：GET/POST 委托 content.service.ts
- 重构 api/agent 其余 8 个文件：移除 prisma 直接调用，委托 service 层
- 新建 services/misc.service.ts、services/user.service.ts
- 扩展 lib/post.ts（事务函数）、lib/upload.ts（文件查询）、lib/tag.ts（标签查询）
- content.service.ts 新增 createPost/updatePost/deletePost/getAgentPosts/getAgentPostDetail
- settings.service.ts 新增 changePassword/uploadAvatar/updateCommentSort

**注意事项**:

- actions/api 层不再直接 import prisma 或跨层调用 lib
- 业务逻辑完全不变，纯架构调整

### 2026-06-21 18:00

**提交人**: AI
**提交哈希**: 1de2f0f
**影响范围**: lib 层（7 个新文件 + 3 个扩展）、services 层（13 个文件重构）
**变更类型**: 重构

**详细描述**:

- 新建 lib 层数据库操作模块：user.ts、post.ts、comment.ts、social.ts、category.ts、tag.ts、settings.ts
- 扩展已有 lib 模块：notification.ts（+4 函数）、token.ts（+4 函数）、webhook.ts（+5 函数）
- 重构全部 13 个 service 文件，移除 prisma 直接调用，改为调用 lib 层函数
- 修复 social.ts 中 findFollowingIds/findFollowerIds 重复定义的严重 bug
- 修复 settings.ts 的 import type 一致性、category.ts 的 select 泛型类型
- 修复 search.service.ts 的命名冲突和类型推断问题
- 修复 notification.service.ts 的 findPostsByIds select 类型推断问题

**注意事项**:

- 本次重构为纯架构调整，业务逻辑无变化
- Service 层不再直接 import prisma，所有数据库操作收敛到 lib 层
- 格式化和类型检查均已通过

### 2026-06-20 16:00

**提交人**: AI
**提交哈希**: 5902805
**影响范围**: 推荐系统（全栈）、文档、配置
**变更类型**: 重构

**详细描述**:

- 移除 Gorse 推荐引擎全部代码，替换为自建 DaLi.Lens 推荐中间件
- 新增 `src/lib/lens.ts`：DaLi.Lens 客户端封装（单例、优雅降级、统一 callLens）
- 重写 `src/services/recommend.service.ts`：基于 Lens API 实现个性化推荐、相似推荐、浏览记录、用户画像
- 重写 `src/actions/recommend.ts`：新增 getSimilarPosts、getUserProfile Actions
- 更新 `src/actions/content.ts`：帖子 CRUD 改用 ingestDocument/updateDocument/deleteDocument
- 更新 `src/services/content.service.ts`：评论反馈改用 submitFeedback(FEEDBACK_ACTION_COMMENT)
- 更新 `src/services/social.service.ts`：点赞/收藏反馈改用 submitFeedback(click/fav)
- 更新 `src/services/auth.service.ts`：注册时异步同步用户到 Lens（createUser）
- 更新 `src/pages/index.astro`：推荐区域改用 isLensEnabled
- 更新 `src/pages/[username]/[postId]/index.astro`：浏览反馈改用 view，新增"相关推荐"区域
- 更新 `src/lib/config.ts`：移除 GORSE_ENDPOINT/GORSE_API_KEY，新增 LENS_ENDPOINT/LENS_API_KEY
- 删除 `src/lib/gorse.ts`，移除 `gorsejs` 依赖
- 更新 `.env.example`、`README.md`、`CLAUDE.md`、`docs/plan/M7-知音.md` 中所有 Gorse 引用
- 修复 `social.service.ts` 中 comment.id 类型错误（select 缺少 id 字段）
- 修复 `recommend.service.ts` 中 visiblePosts 隐式 any 类型

**注意事项**:

- 需配置 LENS_ENDPOINT 和 LENS_API_KEY 环境变量，否则推荐功能静默停用
- 反馈类型映射：bookmark→fav, like→click, read→view, comment→comment
- 取消点赞/取消收藏不发送负向反馈（Lens 无 dislike 类型）

### 2026-06-08 14:00

**提交人**: AI
**提交哈希**: 82365da
**影响范围**: 全站 Actions 客户端调用
**变更类型**: 修复

**详细描述**:

- 修复 Astro 6 Actions 客户端 API 适配问题：Actions 返回 SafeResult（{ data, error }），不抛异常
- 登录/注册页面：错误密码不再直接跳转首页，正确显示错误信息
- 全部 25 个 .astro 文件的 actions 调用从 try/catch 改为 result.error 检查
- 成功数据从 result.data 获取，而非直接从 result 获取
- 修复 BookmarkButton/FollowButton/LikeButton 语法错误（多余缩进和闭合大括号）

**注意事项**:

- Astro 6 Actions 客户端不抛异常，必须检查 result.error
- 此前登录失败时 result.token 为 undefined，被写入 localStorage 导致异常行为

### 2026-06-08 21:30

**提交人**: AI
**提交哈希**: 215e2bc
**影响范围**: 全站 .astro 客户端脚本中的 actions 调用
**变更类型**: 修复

**详细描述**:

- 修复所有 .astro 文件中 `await actions.xxx()` 的客户端代码，适配 Astro 6 SafeResult API
- Astro 6 Actions 客户端 API 返回 `{ data, error }` 而非抛异常，需检查 `result.error` 判断失败
- 成功数据从 `result.data` 获取（如 `result.data.items`、`result.data.success`）
- 移除所有基于 try/catch 的错误处理，改为 `result.error` 检查
- 原来 catch 中检查 `e.status === 401` 改为 `(result.error as any).status === 401`
- 原来 catch 中检查 `e.message` 改为 `result.error.message`
- 不关心返回值的操作（如 updateTheme）直接 await，无需 try/catch
- 修复 BookmarkButton/FollowButton/LikeButton 因之前编辑遗留的多余缩进和闭合大括号导致的语法错误

**涉及文件**（25个）:

- Base.astro, SearchResults.astro, admin/categories.astro, [username]/[postId]/index.astro
- settings/index.astro, notifications.astro, index.astro
- AdminUserList.astro, AdminPostList.astro, AdminTagList.astro, AdminCommentList.astro
- CommentForm.astro, ForumEditor.astro, CommentItem.astro, NotificationList.astro
- ThemeSwitcher.astro, BookmarkButton.astro, LikeButton.astro, FollowButton.astro
- PostEditor.astro, PostEditForm.astro, WebhookManager.astro, TokenManager.astro
- ComposeModal.astro, SearchSuggest.astro, blog/write.astro, [username]/[postId]/edit.astro

**注意事项**:

- login.astro 和 register.astro 已在之前修复，无需再改
- 所有修改仅涉及客户端脚本中的 actions 调用模式，不影响服务端逻辑

### 2026-06-08 13:26

**提交人**: AI
**提交哈希**: d5da615
**影响范围**: 登录/注册/登出
**变更类型**: 修复

**详细描述**:

- 修复登录后用户菜单不显示的问题：移除客户端 `document.cookie` 设置，避免覆盖服务端 HttpOnly cookie
- 登录/注册页面不再手动设置 cookie，完全依赖服务端 `setTokenCookie` 设置的 HttpOnly cookie
- 登出时增加 `document.cookie` 清除，确保残留的客户端 cookie 被清理

**注意事项**:

- SSR 页面通过 HttpOnly cookie 读取用户状态，客户端通过 localStorage 读取 token

### 2026-06-08 13:12

**提交人**: AI
**提交哈希**: 25e5e07
**影响范围**: 导航栏
**变更类型**: 优化

**详细描述**:

- 导航栏改为三栏布局：品牌（左）| 频道导航（居中）| 操作区（右）
- 未登录用户右上角显示登录🔑/注册📝图标按钮，点击跳转对应页面
- 频道导航居中显示，仅启用一个模块时不显示（`isSingleMode` 控制）
- 移动端恢复 hamburger 切换按钮，修复 JS 中 ID 引用与 HTML 不匹配的问题
- 清理 Base.astro 中 layoutMode 残留代码，改为 narrow 布尔值

**注意事项**:

- 单模式站点不显示频道导航链接
- 移动端通过 hamburger 面板访问导航和登录注册

### 2026-06-08 05:00

**提交人**: AI
**提交哈希**: 29b352d
**影响范围**: 全站模式名称显示
**变更类型**: 新增

**详细描述**:

- 新增环境变量 SITE_MODE_WEIBO / SITE_MODE_FORUM / SITE_MODE_BLOG，支持自定义模式显示别名
- 新增 `getModeLabel()` 函数（src/lib/config.ts），统一获取模式显示名称
- 15 个文件中的硬编码"微博/论坛/博客"替换为 `getModeLabel()` 调用
- .env.example 添加模式别名配置说明

**注意事项**:

- 未设置别名环境变量时，默认显示名称不变（微博/论坛/博客）
- 路由路径和 mode 数据标识不受影响

### 2026-06-07 20:15

**提交人**: AI
**提交哈希**: a800edc
**影响范围**: 首页布局
**变更类型**: 重构

**详细描述**:

- 新增 HomeLayout（居中 960px 无侧边栏），首页作为全站数据聚合入口
- 首页从 WeiboLayout 切换到 HomeLayout，移除侧边栏
- 首页标题改为"发现"，副标题根据登录状态显示个性化提示
- 用户主页从 WeiboLayout 切换到 UserLayout（居中 800px 无侧边栏）

### 2026-06-07 20:10

**提交人**: AI
**提交哈希**: 0242c06
**影响范围**: 布局系统、Service 层、搜索功能、用户界面
**变更类型**: 重构 | 修复 | 新增

**详细描述**:

- 引入 Service 层架构，消除 Actions 与 API 的业务逻辑重复
    - 新增 src/lib/errors.ts（ServiceError 错误类）
    - 新增 src/services/ 目录，包含 15 个业务编排模块
    - 所有 Actions 改造为薄适配层（鉴权 + zod 校验 + 委托 service）
    - 8 个 Agent API 改为调用 service
- 修复用户菜单下拉无效问题（script 标签自定义属性导致 Vite 跳过处理）
- 移除导航栏和移动端 Tab Bar 的发布按钮，各频道标题旁添加发布按钮
- 搜索建议增加帖子结果（searchSuggest 增加 posts 查询）
- 拆分布局系统：BaseLayout 为纯页面骨架，新增 4 个子 Layout
    - WeiboLayout：两栏布局（侧边栏 + 内容），用于首页/微博/关注/收藏等
    - ForumLayout：全宽 1200px 无侧边栏，用于论坛频道
    - BlogLayout：全宽 1200px 无侧边栏，用于博客频道
    - UserLayout：居中 800px 无侧边栏，用于用户主页/帖子详情/编辑/版本历史

**注意事项**:

- BaseLayout 不再处理侧边栏，由子 Layout 各自管理
- layoutMode 属性仅保留 'narrow'（登录/注册页），其他页面使用子 Layout
