# M6 百变 — 分类系统 + 多模式 + 收藏 + Actions 重构

> 里程碑代号：M6
> 核心目标：引入分类与多模式架构，使睦谈可按需变形为微博/论坛/博客；迁移到 Astro Actions；去除 React 依赖，回归原生 Astro 组件
> 前置依赖：M1-M5 全部完成
> 执行时间：2026-06

---

## 背景与动机

睦谈 M1-M5 已完成完整的微博功能闭环。但微博、论坛、博客在内容组织上有大量共性——用户、帖子、评论、互动——差异主要在**展示方式**和**编辑体验**。

通过引入**分类模式**概念，让一个站点同时支持多种内容形态：

- 微博频道：时间线消费，快速短文
- 论坛频道：版块浏览，结构化讨论
- 博客频道：文章阅读，长文写作

管理员通过 `SITE_MODES` 配置启用哪些模式，至少选一种。只选一种时等效于纯微博/纯论坛/纯博客。

同时，M6 将进行两项架构优化：

1. **API → Astro Actions**：内部交互从 `fetch('/api/...')` 迁移到 Astro Actions，获得类型安全和更简的代码
2. **React Islands → 原生 Astro**：去除 React 依赖，使用 Astro 原生组件 + Actions 实现交互

---

## 核心设计决策

### 决策 1：模式由分类决定，而非站点全局

每个分类（Category）携带 `mode` 字段（weibo/forum/blog），帖子发布时继承分类的模式。无分类的帖子默认为 weibo 模式。

**理由**：比全局 SITE_MODE 更灵活，一站可多形态混搭。

### 决策 2：帖子冗余存储 mode 字段

Post 表直接存储 `mode`，不通过 categoryId JOIN 推导。

**理由**：

- 无分类帖子也需要模式标识
- 首页混合时间线查询无需 JOIN
- 发布时写入，之后不变

### 决策 3：title 字段多语义

| mode  | title 含义    | 必填 |
| ----- | ------------- | ---- |
| weibo | 帖子摘要/简介 | 否   |
| forum | 帖子标题      | 是   |
| blog  | 文章标题      | 是   |

### 决策 4：发布入口绑定频道

在哪个频道页面下发布，就使用该模式的编辑器，帖子自动带该 mode。不提供跨模式发布。

### 决策 5：站点模式配置不可变

`SITE_MODES` 在 `.env` 中配置，建站时确定，运行期间不可更改。分类管理只能创建已启用模式的分类。

### 决策 6：现有 API 全部标记弃用

所有现有 `/api/` 路由标记为 `@deprecated`，M6 不再新增 API 路由（分类管理走 Actions）。API 路由将在后续里程碑中统一整理升级，当前仅保留不动。

### 决策 7：优先使用原生 Astro 组件

交互组件（点赞、关注、收藏等）使用 Astro 原生组件 + Astro Actions 实现，不再依赖 React Islands。仅博客富文本编辑器（Tiptap）因复杂度保留为 React Island。

**理由**：

- 减少客户端 JS 体积（无 React runtime）
- Astro Actions + `<form>` 天然支持渐进增强
- 代码更统一，维护更简单

---

## 页面结构

### 多模式启用时

| 页面       | 路由                  | 说明                                           |
| ---------- | --------------------- | ---------------------------------------------- |
| 大杂烩首页 | `/`                   | 混合时间线，三种模式帖子混排，卡片按 mode 渲染 |
| 微博频道   | `/weibo`              | 时间线 + 简单发布框                            |
| 论坛频道   | `/forum`              | 版块列表 → 帖子列表 + 结构化发布框             |
| 博客频道   | `/blog`               | 文章列表 + 富文本发布框                        |
| 个人主页   | `/@username`          | 按用户帖子 mode 分 tab                         |
| 内容详情   | `/@username/{postId}` | 根据 post.mode 渲染不同模板                    |

### 单模式启用时

`/` 直接重定向到该模式首页（如只启用 weibo → `/` 就是 `/weibo` 的内容），导航不显示频道切换。

---

## Schema 变更

### 新增表

#### Category（分类）

```prisma
/// 分类表 — 两级结构，一级为分组，二级为分类
model Category {
  id          String     @id @default(cuid())
  name        String     // 分类名称
  slug        String     @unique  // URL 友好标识
  mode        String     // weibo | forum | blog — 分类所属模式
  parentId    String?    // 父分类 ID（null = 一级分组）
  description String     @default("")  // 分类描述
  icon        String     @default("")  // 图标（emoji 或图标名）
  sortOrder   Int        @default(0)   // 排序权重
  postCount   Int        @default(0)   // 帖子数缓存
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt

  parent      Category?  @relation("CategoryChildren", fields: [parentId], references: [id])
  children    Category[] @relation("CategoryChildren")
  posts       Post[]
}
```

#### Bookmark（收藏）

```prisma
/// 收藏表 — 用户收藏/书签帖子
model Bookmark {
  id        String   @id @default(cuid())
  userId    String   // 收藏者
  postId    String   // 被收藏的帖子
  createdAt DateTime @default(now())

  user      User     @relation(fields: [userId], references: [id])
  post      Post     @relation(fields: [postId], references: [id])

  @@unique([userId, postId])  // 每人每帖只能收藏一次
}
```

### Post 表新增字段

```prisma
model Post {
  // ... 现有字段 ...

  mode        String    @default("weibo")  // weibo | forum | blog — 帖子模式
  title       String?   // weibo=可选摘要, forum/blog=标题
  categoryId  String?   // 外键 → Category

  category    Category? @relation(fields: [categoryId], references: [id])
  bookmarks   Bookmark[]
}
```

### User 表新增关联

```prisma
model User {
  // ... 现有字段 ...

  bookmarks   Bookmark[]
}
```

---

## 站点配置

### 新增环境变量

| 环境变量     | 类型   | 默认值    | 说明                                                         |
| ------------ | ------ | --------- | ------------------------------------------------------------ |
| `SITE_MODES` | string | `"weibo"` | 启用的模式，逗号分隔，至少选一个。可选值：weibo, forum, blog |

### 读取方式

在 `src/lib/config.ts` 中解析：

```typescript
/** 解析启用的站点模式 */
const SITE_MODES = (import.meta.env.SITE_MODES || 'weibo')
	.split(',')
	.map((m) => m.trim())
	.filter((m) => ['weibo', 'forum', 'blog'].includes(m));

/** 是否启用某模式 */
const isModeEnabled = (mode: string) => SITE_MODES.includes(mode);

/** 是否为单模式站点 */
const isSingleMode = SITE_MODES.length === 1;

/** 单模式时的模式值 */
const singleMode = isSingleMode ? SITE_MODES[0] : null;
```

---

## 各模式行为差异

| 维度       | weibo          | forum                | blog                                          |
| ---------- | -------------- | -------------------- | --------------------------------------------- |
| 分类定位   | 可选话题       | 必选版块             | 文章栏目                                      |
| title 字段 | 可选摘要       | 必填标题             | 必填标题                                      |
| 编辑器     | 纯文本框       | 简易 Markdown 编辑器 | 所见即所得富文本（Tiptap，唯一 React Island） |
| 帖子卡片   | 内容为主       | 标题 + 回复数        | 标题 + 摘要 + 封面                            |
| 评论排序   | 时间倒序       | 热度+时间            | 时间倒序                                      |
| 用户主页   | 时间线         | 主帖 + 回帖          | 文章列表                                      |
| 首页布局   | 时间线         | 版块列表 → 帖子列表  | 文章卡片列表                                  |
| 导航项     | 首页/通知/我的 | 首页/版块/热门/我的  | 首页/分类/归档/关于                           |

---

## React → Astro 迁移方案

### 现有 React Islands

| 组件         | 用途          | 迁移方案                                            |
| ------------ | ------------- | --------------------------------------------------- |
| LikeButton   | 点赞/取消点赞 | Astro 组件 + `<form action={actions.toggleLike}>`   |
| FollowButton | 关注/取关     | Astro 组件 + `<form action={actions.toggleFollow}>` |
| PostEditor   | 微博发帖      | Astro 组件 + `<form action={actions.createPost}>`   |
| PostEditForm | 编辑帖子      | Astro 组件 + `<form action={actions.updatePost}>`   |

### 保留 React 的场景

仅 **BlogEditor**（Tiptap 富文本编辑器）保留为 React Island，因为：

- 所见即所得编辑器需要复杂的状态管理
- Tiptap 是 React 生态组件
- 仅 blog 模式加载，不影响其他模式

### 迁移后的交互模式

```
之前：Astro 页面 → React Island → fetch('/api/...') → API 路由 → prisma
之后：Astro 页面 → Astro 组件 + <form action> → Astro Actions → prisma
特例：博客编辑器 → React Island (Tiptap) → Astro Actions → prisma
```

---

## 任务清单

### T1 分类系统

**目标**：建立分类数据模型和管理能力

**步骤**：

1. Schema 新增 Category 表
2. Post 表新增 categoryId、mode、title 字段
3. 分类管理 Actions（`actions.createCategory` / `updateCategory` / `deleteCategory`）
4. 管理后台分类管理页（`/admin/categories`）— 原生 Astro 组件
5. 帖子发布/编辑 Actions 适配 categoryId 和 mode
6. 分类帖子数缓存更新逻辑

**效果**：管理员可创建/编辑/删除分类，帖子可关联分类

**验证**：

- 管理后台可创建两级分类，指定 mode
- 帖子发布时可选择分类
- 分类列表页正确显示帖子数

---

### T2 帖子模式适配

**目标**：Post 的 mode/title 字段在各场景下正确工作

**步骤**：

1. 发布 Action 根据 mode 校验 title 必填性
2. 帖子列表查询返回 mode 和 title
3. 帖子详情页根据 mode 渲染不同模板
4. 帖子卡片组件根据 mode 切换展示样式
5. 用户主页按 mode 分 tab 展示
6. 搜索结果按 mode 展示差异

**效果**：三种模式的帖子在展示上有明确区分

**验证**：

- forum/blog 模式帖子无标题时发布失败
- weibo 模式帖子卡片以内容为主
- forum 模式帖子卡片以标题为主
- blog 模式帖子卡片展示标题+摘要

---

### T3 频道页面

**目标**：实现三个频道独立页面 + 大杂烩首页 + 导航

**步骤**：

1. `SITE_MODES` 配置解析逻辑
2. 大杂烩首页 `/` — 混合时间线（多模式时）
3. 微博频道 `/weibo` — 时间线 + 简单发布框
4. 论坛频道 `/forum` — 版块列表 → 帖子列表
5. 博客频道 `/blog` — 文章列表
6. 单模式时 `/` 重定向到该模式首页
7. 导航栏根据 SITE_MODES 动态渲染频道入口
8. 各频道页面内嵌对应模式的发布入口

**效果**：用户可通过导航切换频道，在各频道内发布对应模式的内容

**验证**：

- 多模式时首页显示混合时间线
- 单模式时首页直接是该模式首页
- 微博频道下发布使用简单编辑器
- 论坛频道下发布必须选版块和标题
- 博客频道下发布使用富文本编辑器
- 导航只显示已启用的频道

---

### T4 收藏/书签

**目标**：支持帖子收藏

**步骤**：

1. Schema 新增 Bookmark 表
2. 收藏 Action（`actions.toggleBookmark`）— 切换模式
3. 收藏列表页（`/bookmarks`）— 原生 Astro 组件
4. 帖子详情页收藏按钮 — Astro 组件 + `<form action>`
5. 收藏数统计

**效果**：用户可收藏帖子，查看收藏列表

**验证**：

- 可收藏/取消收藏帖子
- 收藏列表正确展示
- 收藏按钮状态正确

---

### T5 Actions 迁移 + React 去除

**目标**：将内部交互从 API 迁移到 Astro Actions，同时将 React Islands 替换为原生 Astro 组件

**步骤**：

**阶段 1：创建 Actions 基础设施**

1. 创建 `src/actions/index.ts`，定义所有 action
2. 配置 Astro Actions（确保 `astro.config.ts` 已启用）

**阶段 2：迁移交互逻辑** 3. 迁移点赞 → `actions.toggleLike` 4. 迁移关注 → `actions.toggleFollow` 5. 新增收藏 → `actions.toggleBookmark` 6. 迁移发帖 → `actions.createPost` 7. 迁移编辑 → `actions.updatePost` 8. 迁移删除 → `actions.deletePost` 9. 迁移评论 → `actions.createComment` / `deleteComment` 10. 迁移登录/注册 → `actions.login` / `actions.register` 11. 迁移其他交互（置顶、锁定、密码验证、设置等）

**阶段 3：替换 React Islands** 12. LikeButton.tsx → Astro 组件 + `<form action={actions.toggleLike}>` 13. FollowButton.tsx → Astro 组件 + `<form action={actions.toggleFollow}>` 14. PostEditor.tsx → Astro 组件 + `<form action={actions.createPost}>` 15. PostEditForm.tsx → Astro 组件 + `<form action={actions.updatePost}>` 16. 删除 React 相关依赖（`@astrojs/react`、`react`、`react-dom`），保留给 BlogEditor

**阶段 4：标记 API 弃用** 17. 所有 `/api/` 路由文件头部添加 `@deprecated` 注释 18. API 路由代码不动，仅标记，后续里程碑统一整理

**迁移优先级**：

| 优先级 | Action           | 原 API 路由                           | 原组件           |
| ------ | ---------------- | ------------------------------------- | ---------------- |
| P1     | `toggleLike`     | `PUT /api/posts/:id/like`             | LikeButton.tsx   |
| P1     | `toggleFollow`   | `PUT /api/users/:username/follow`     | FollowButton.tsx |
| P1     | `toggleBookmark` | 新增                                  | 新组件           |
| P2     | `createPost`     | `POST /api/posts`                     | PostEditor.tsx   |
| P2     | `updatePost`     | `PUT /api/posts/:id`                  | PostEditForm.tsx |
| P2     | `deletePost`     | `DELETE /api/posts/:id`               | `<script>`       |
| P2     | `createComment`  | `POST /api/comments`                  | `<script>`       |
| P2     | `deleteComment`  | `DELETE /api/comments/:id`            | `<script>`       |
| P3     | `login`          | `POST /api/auth/login`                | `<script>`       |
| P3     | `register`       | `POST /api/auth/register`             | `<script>`       |
| P3     | `updateProfile`  | `PUT /api/settings/profile`           | `<script>`       |
| P3     | `verifyPassword` | `POST /api/posts/:id/verify-password` | `<script>`       |
| P3     | `togglePin`      | `PUT /api/posts/:id/pin`              | `<script>`       |
| P3     | `toggleLock`     | `PUT /api/posts/:id/lock`             | `<script>`       |

**效果**：内部交互走 Actions，React 仅用于博客编辑器，API 标记弃用

**验证**：

- 所有交互功能正常（点赞、关注、发帖、评论等）
- 无 React Island 组件（除 BlogEditor）
- 客户端无 React runtime（除博客编辑器页面）
- Actions 返回类型正确推导
- API 路由文件有 `@deprecated` 标记

---

### T6 文章编辑器（blog 模式）

**目标**：三种模式使用完全不同的编辑器

**编辑器规格**：

| 模式  | 编辑器类型           | Markdown 支持                   | 实现方式                | 参考体验                  |
| ----- | -------------------- | ------------------------------- | ----------------------- | ------------------------- |
| weibo | 纯文本框             | 粗体/斜体/删除线/行内代码/链接  | Astro 组件 + `<form>`   | 微博/推特发帖框           |
| forum | 简易 Markdown 编辑器 | 标题/列表/引用/代码块/链接/图片 | Astro 组件 + `<script>` | Discourse / GitHub 评论框 |
| blog  | 所见即所得富文本     | 全部 Markdown + block 编辑      | React Island（Tiptap）  | Notion / 语雀             |

**步骤**：

1. 安装 Tiptap 核心及扩展（starter-kit、image、code-block-lowlight、link、placeholder、markdown）
2. 创建 BlogEditor 组件（React Island）— block 编辑 + 工具栏
3. 创建 ForumEditor 组件（Astro 组件）— 简易 Markdown 编辑器 + 预览，用 `<script>` 处理交互
4. Tiptap 内容与 Markdown 双向转换（tiptap-markdown 扩展）
5. 博客频道发布/编辑页使用 BlogEditor
6. 论坛频道发布/编辑页使用 ForumEditor
7. 微博频道使用 Astro 原生 `<form>` + textarea

**效果**：三种模式编辑体验完全不同，博客有 Notion 级别的 block 编辑体验

**验证**：

- 微博编辑器：纯文本框，支持基础 Markdown
- 论坛编辑器：工具栏 + 实时预览，支持标题/列表/代码块等
- 博客编辑器：所见即所得，block 编辑，工具栏完整
- 三种编辑器内容均正确保存为 Markdown
- 编辑已有内容时正确回填

---

### T7 搜索增强

**目标**：搜索支持按 mode/分类过滤

**步骤**：

1. 搜索 Action 新增 `mode` 和 `categoryId` 过滤参数
2. 搜索页增加模式/分类筛选 UI
3. 实时建议 Action（`actions.searchSuggest`）
4. 建议结果包含热门标签 + 用户 + 分类

**效果**：搜索可按模式和分类过滤，输入时有实时建议

**验证**：

- 搜索结果可按 mode 过滤
- 搜索结果可按分类过滤
- 输入关键词时显示建议

---

## 依赖关系与执行顺序

```
T1(分类系统) → T2(帖子模式) → T3(频道页面) → T6(编辑器)
T4(收藏)                          ← 可与 T1-T3 并行
T5(Actions 迁移 + React 去除)     ← 建议优先执行，为其他任务铺路
T7(搜索增强) ← T1                 ← 后置
```

推荐执行顺序：

```
第一批：T5（Actions 迁移 + React 去除）— 基础设施变更，先做
第二批：T1 + T4（分类 + 收藏，并行）
第三批：T2（帖子模式，依赖 T1 + T4）
第四批：T3（频道页面，依赖 T1 + T2）
第五批：T6（编辑器，依赖 T3）
第六批：T7（搜索增强，依赖 T1）
```

---

## 技术风险与应对

| 风险                       | 影响           | 应对                                                |
| -------------------------- | -------------- | --------------------------------------------------- |
| 富文本编辑器体积大         | 首屏加载慢     | React Island 懒加载，仅 blog 模式加载               |
| 分类帖子数缓存一致性       | 数据不准       | 写入时更新 + 定时校准                               |
| 模式切换导致 UI 复杂度上升 | 维护成本高     | 抽取模式相关逻辑为独立组件，按 mode 条件渲染        |
| 首页混合时间线查询性能     | 多模式 JOIN 慢 | mode 冗余存储避免 JOIN，分页查询加索引              |
| Actions + React 去除范围大 | 回归风险       | 分批迁移，每批验证，API 路由保留作为回退            |
| ForumEditor 无 React       | 交互体验降级   | 用 `<script>` + 原生 DOM 实现工具栏和预览，够用即可 |

---

## 新增环境变量汇总

| 变量         | 类型   | 默认值    | 说明                                                   |
| ------------ | ------ | --------- | ------------------------------------------------------ |
| `SITE_MODES` | string | `"weibo"` | 启用的模式，逗号分隔（weibo, forum, blog），至少选一个 |

---

## API 处理策略

**现有 API 路由**：全部标记 `@deprecated`，代码不动，后续里程碑统一整理升级。

**新增交互**：全部走 Astro Actions，不再新增 API 路由。

**API 路由保留价值**：供外部客户端（API Token 认证）使用，M6 不删除，仅标记弃用。

---

## 新增页面汇总

| 页面       | 路由                | 说明              |
| ---------- | ------------------- | ----------------- |
| 大杂烩首页 | `/`                 | 多模式混合时间线  |
| 微博频道   | `/weibo`            | 微博时间线 + 发布 |
| 论坛频道   | `/forum`            | 版块列表          |
| 论坛版块   | `/forum/:slug`      | 版块内帖子列表    |
| 博客频道   | `/blog`             | 文章列表          |
| 博客分类   | `/blog/:slug`       | 分类内文章列表    |
| 收藏列表   | `/bookmarks`        | 用户收藏的帖子    |
| 分类管理   | `/admin/categories` | 管理后台分类 CRUD |
